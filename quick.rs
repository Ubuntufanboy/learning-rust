fn main() {
    let mut number: i32 = 37;
    let mut number: i64 = 639; // You are allowed to change type with let
    number = 10 // This is allowed! because the variable is mutable and it isnt changing datatypes
    //number = 10 // number is no longer mutable since let reasigned the properties of number
    //number: i64 = 120; // Now allowed

}